import {ScaledSheet} from 'react-native-size-matters';
import Colors from '../../../Infrastructure/Helper/Utils/Colors';

export default ScaledSheet.create({
  txtSelectInput: {
    backgroundColor: Colors.lightPinky,
    borderRadius: 8,
    marginBottom: '15@msr',
  },
  txtSelectInputText: {
    color: Colors.darkblue,
    fontSize: '15@msr',
  },
});
